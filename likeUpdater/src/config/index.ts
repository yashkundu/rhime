// The size of batch in which the events are pulled by the pull consumer
export const EVENT_BATCH_SIZE=5

// The no.of milliseconds to store the eventId in redis before expiration
export const EVENT_TTL=1000*60*15
